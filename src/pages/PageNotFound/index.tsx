import NotFound from '@/components/NotFound'
import styles from './styles'

function PageNotFound() {
  return (
    <main className={styles.container}>
      <NotFound variant="page" />
    </main>
  )
}

export default PageNotFound
